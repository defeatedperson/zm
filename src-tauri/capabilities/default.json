{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口与各壁纸窗口的默认能力",
  "windows": ["main", "wallpaper-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "autostart:allow-is-enabled",
    "autostart:allow-enable",
    "autostart:allow-disable"
  ]
}
